export class Client {
  fullname: string;
  email: string;
  phone: string;

  constructor(fullname: string, email: string, phone: string) {
    this.fullname = fullname;
    this.email = email;
    this.phone = phone;
  }
}
